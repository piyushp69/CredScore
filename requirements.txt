# Core: pipeline, model, API and dashboard
numpy>=1.26
pandas>=2.2
pyarrow>=15
scikit-learn>=1.4
xgboost>=2.1
pydantic>=2.6
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.52  # st.metric(delta_arrow=...)
plotly>=5.24

# Dataset download from Kaggle (optional if the CSVs are already in dataset/)
kagglehub>=0.3

# Tests
pytest>=8
httpx>=0.27
